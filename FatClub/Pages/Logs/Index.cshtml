@page
@model FatClub.Pages.Logs.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AuditLog[0].AuditActionType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuditLog[0].Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuditLog[0].DateTimeStamp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AuditLog.Reverse()) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AuditActionType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTimeStamp)
            </td>
            <td>
        <!--<a asp-page="./Edit" asp-route-id="@item.Audit_ID">Edit</a> | -->
            <a asp-page="./Details" asp-route-id="@item.Audit_ID">Details</a> <!--|
        <a asp-page="./Delete" asp-route-id="@item.Audit_ID">Delete</a>-->
            </td>
        </tr>
}
    </tbody>
</table>
